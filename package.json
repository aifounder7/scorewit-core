{
  "name": "@scorewit/core",
  "version": "0.2.0",
  "description": "Sport-agnostic engine for Scorewit daily trivia apps: deterministic ingest → generate → validate → render pipeline, single-file app shell, seeded RNG, streak model, and the generic SportPack interface.",
  "license": "MIT",
  "author": "Jay",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "tsx src/streak.test.ts"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "tsx": "^4.22.4",
    "typescript": "~6.0.3"
  }
}
